# Qdrant server config (server profile, §4). Embedded profile uses on-disk local mode.
storage:
  storage_path: /qdrant/storage
  optimizers:
    default_segment_number: 2
service:
  http_port: 6333
  grpc_port: 6334
